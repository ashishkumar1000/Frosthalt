# Deferred Work

Collected during story review. Pre-existing or latent items not caused by the current story, surfaced incidentally.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-react-native-macos-app.md`
  summary: Jest `transformIgnorePatterns` does not match `react-native-macos` (hyphen-suffix) or arbitrary `@react-native-*` scoped packages, so any future test importing them directly would fail to transform.
  evidence: The allow-list boundary is `(@|/|\+)`; `react-native-macos` has a `-` after `react-native`, so it falls through to the ignore side and is not transformed. No current test imports `react-native-macos` directly (tests import `react-native`, which the pattern does match, and the suite is green), so nothing breaks today. The default `react-native` preset has the same gap, so this was not introduced by this story — but this is a react-native-macos project, so a later test that imports `react-native-macos` directly would hit `SyntaxError: Cannot use import statement outside a module`. Trivial to harden later by widening the allow-list to `react-native[\w-]*` / `@react-native[\w-]*`.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-react-native-macos-app.md`
  summary: No automated native-build verification (CI) for the macOS target — AC 1 (New Arch by default) and AC 2 (`npm run macos` launches a window) rely on a one-time human run.
  evidence: The only test in the repo is `__tests__/App.test.tsx`, which runs Jest on JS only and never touches `pod install`/xcodebuild. No project CI exists (the only `.github/workflows` files in the repo belong to vendored Ruby gems). The spec explicitly sanctions human-run verification for AC 2 ("the sandbox cannot run xcodebuild"), so this is not a spec gap — it is a project-level gap surfaced incidentally. A macOS CI step running `pod install` + `xcodebuild build` for the `Frosthalt-macOS` target is the natural way to pin the Podfile flag and the native build.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-react-native-macos-app.md`
  summary: New Architecture enablement has no single source of truth across targets — macOS pins `:fabric_enabled => true` in the Podfile, Android uses `android/gradle.properties` `newArchEnabled`, and the iOS Podfile does not set `:fabric_enabled` at all, so a contributor cannot tell whether iOS and macOS end up in the same architecture state.
  evidence: Only the macOS Podfile was touched by Story 1.1 (macOS-only scope; ios/android removal is explicitly out of scope per the spec's Ask First). The `ios/Podfile` and `android/gradle.properties` predate this story (scaffold commit f7419d5). On RN 0.81 the New Architecture is default-on, but the iOS target's Fabric state still depends on the unmodified `ios/Podfile` + ENV, which is not reconciled with the macOS pin. Real, pre-existing, surfaced incidentally.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-react-native-macos-app.md`
  summary: The known macOS `pod install` header-staging failure (`'glog/logging.h' file not found`) and its clean-reinstall remedy are documented nowhere in the repository — only in project memory — so a contributor running `pnpm run macos` before a clean `pod install` hits it with no in-repo guidance.
  evidence: Story 1.1 added the `macos` script (`package.json:9`) and the README already documents macOS setup (commit 8b5aacd), but neither covers the glog header-staging failure or the `pod cache clean --all && rm -rf Pods Podfile.lock && pod install` remedy. The failure recurred during this story and is real (CocoaPods 1.16.2 + Xcode 26 skips `Pods/Headers/Public/glog/` symlink staging). Pre-existing tooling gap, surfaced by the story adding the run command.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-react-native-macos-app.md`
  summary: `react-native-macos` is a caret range (`^0.81.9`) while core `react-native` is pinned exact (`0.81.2`); an out-of-tree platform package drifting ahead of the core RN minor it must binary-match is a classic build/runtime breakage source.
  evidence: The version specifiers are pre-existing (set by the original scaffold, commit f7419d5; Story 1.1 did not change versions — the removed line was a malformed `0.81.0` devDependency duplicate, not the real `^0.81.9` dependency, which stays). Real latent risk: a `pnpm update react-native-macos` could lift it to 0.81.10+ while core stays 0.81.2, breaking the ABI match the out-of-tree platform requires. Not caused by this story.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffold-react-native-macos-app.md`
  summary: `macos/Podfile` calls `flags = get_default_flags()` and keeps the comment "Flags change depending on the env values.", but `flags` is never used — `:fabric_enabled` is hardcoded, so `get_default_flags` (which reads `RCT_NEW_ARCH_ENABLED`) is dead and misleading.
  evidence: The `flags = get_default_flags()` line and comment are unchanged context in Story 1.1's diff (pre-existing, present at baseline c3ab4fa). `flags` is not referenced by `use_react_native!` (which takes `:fabric_enabled => true` and `:app_path` directly), so it has been dead since before this story. Trivial cleanup in a file this story edits.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-design-token-system-and-shared-visual-primitives.md`
  summary: White label text (#FFFFFF) over `systemOrangeColor`/`systemRedColor` (the amber/blocked status fills) may fall below WCAG AA 4.5:1 contrast in light appearance — the palette is owned by the frozen DESIGN.md and was not introduced or changeable by this story.
  evidence: `primaryForeground` is `#FFFFFF` (frozen) and the amber/blocked fills resolve to `systemOrangeColor`/`systemRedColor` (frozen). `#FFFFFF` on `#FF9500` (the nominal systemOrange hex) is ~1.9:1, well under AA; on `#FF3B30` (systemRed) it is ~3.3:1, still under 4.5:1. The label typography is 11pt/500 (label token), below the 14pt-or-bold AA threshold that relaxes to 3:1. This is a real contrast concern, but the palette is human-owned in DESIGN.md's frozen frontmatter and the Story 1.2 Boundaries pin the NSColor mappings, so the fix (darker fills, or a fill-dependent foreground) is a DESIGN.md renegotiation, not a patch here. Surfaced incidentally by the StatusBadge fill-wiring review; the systemGreen `#34C759` fill is closer (~2.6:1, still under) but the free state is less critical. No automated contrast assertion exists because the fills are opaque PlatformColor objects at runtime; a contrast check belongs in a later design-audit story.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-design-token-system-and-shared-visual-primitives.md`
  summary: `ApplyButton` has no hover or keyboard-focus visual affordance for the macOS pointer/keyboard — only the press and disabled states are in this story's scoped primitive, and the window default-button pulse/Return binding is already carried to Story 1.3.
  evidence: `ApplyButton.tsx` styles only `pressed` (opacity 0.85) and `disabled` (opacity 0.4); there is no `hovered`/`focused` style and no `onHoverIn`/`onFocus` handler. macOS surfaces are pointer-and-keyboard-first, so a primary action button without a hover or focus ring feels wrong on the desktop. The frozen I/O matrix rows for ApplyButton are only idle and disabled (no hover/focus row), and the Boundaries explicitly defer the default-button pulse + Return binding to Story 1.3 — so hover/focus affordance is out of scope here by the same line, not a gap in this story. Real future need: Story 1.3's window shell should add a hover/focus style (and the keyboarding API for Return), keeping the press/disabled primitive from 1.2 intact.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-design-token-system-and-shared-visual-primitives.md`
  summary: The token module defines no `labelForeground`/`secondaryForeground` tokens for text on default (window-background) surfaces — only `primaryForeground` (white, for saturated fills), so downstream components rendering text on the default surface have no named foreground token to import.
  evidence: `tokens.ts` exports `primaryForeground: '#FFFFFF'` (documented as "white over saturated fills") plus the `monoBg`/`monoFg` mono-viewer pair. There is no token for the macOS default label color (`labelColor`) or a muted secondary text. Story 1.2's two components (`StatusBadge`, `ApplyButton`) only ever render white text over a saturated fill, so they do not need a default-surface foreground — the story has no consumer for it. But the later window shell, sidebar rows, and hosts viewer (stories 1.3–1.6) will render text on the default window background and will need this; introducing it now with no consumer would be speculative. Real future need; defer to the first story that renders text on a default surface (likely 1.3), which should add `labelForeground: PlatformColor('labelColor')` and a `secondaryForeground` (e.g. `tertiaryLabelColor` or `secondaryLabelColor`) to the token module.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sidebar-navigation-and-status-header-shell.md`
  summary: The sidebar supports only ⌘1–⌘4 navigation; there is no ArrowUp/ArrowDown handling, the conventional macOS/VoiceOver pattern for a vertical list that keyboard users reach for first.
  evidence: The frozen Boundaries scope keyboard nav to ⌘1–⌘4 (`onKeyDown` gating on `metaKey` + key `1`–`4`); arrow keys are not in the I/O Matrix and the AC names only ⌘1–⌘4. `keyDownEvents` declares only the four ⌘-key entries. Arrow-key nav is a real a11y improvement but out of scope for this story; defer to a later a11y/keyboard-polish pass (add ArrowUp/ArrowDown to `keyDownEvents` + `onKeyDown`, with tests).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sidebar-navigation-and-status-header-shell.md`
  summary: The sidebar has no background material/vibrancy — it shares the window background, so the navigation column has no visual boundary from the content area (macOS sidebars are typically vibrancy/material-distinct).
  evidence: The frozen Boundaries say "unselected rows use the default surface" (no special sidebar background); DESIGN.md specifies the ~180px sidebar and RN-primitive construction but no sidebar material. react-native-macos exposes no native sidebar control (per DESIGN.md), and a `VisualEffectView`/vibrancy material would need a native module or library — the frozen Boundaries require "Ask First" before any native module. Out of scope for this presentational shell; defer to a visual-polish story (may need a small native vibrancy module).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sidebar-navigation-and-status-header-shell.md`
  summary: `StatusHeader` has no bottom hairline/separator from the content area below it; the persistent status bar runs into the surface placeholder with no visual divider.
  evidence: The frozen Boundaries pin the status header's CONTENT (`[StatusBadge "free"] · "0 domains" · "no active timer"`) but not any border/divider; DESIGN.md calls it "a compact bar pinned above the content area" without specifying a divider. Out of scope for this story's placeholder header; defer to a visual-polish pass when the header gets real state (Epic 2/4).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sidebar-navigation-and-status-header-shell.md`
  summary: The middle-dot separators in `StatusHeader` are standalone `<Text>` nodes that VoiceOver may read aloud ("middle dot") between "Free", "0 domains", and "no active timer".
  evidence: `StatusHeader.tsx` renders `<Text style={styles.separator}>·</Text>` as sibling Text nodes. The frozen a11y path for this story is `AccessibilityInfo.announceForAccessibility` for nav announcements; it does not require the status header itself to be VoiceOver-optimized. Hiding the separators (e.g. `accessibilityElementsHidden`/`importantForAccessibility="no-hide-descendants"`) or exposing a single combined `accessibilityLabel` on the header is the fix, but those props' macOS support is not verified in this story's API anchors. Defer to an a11y-polish pass with on-device prop verification.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sidebar-navigation-and-status-header-shell.md`
  summary: The sidebar `View` has no `accessibilityRole="list"` and the rows are `accessibilityRole="button"` rather than `listitem`, so VoiceOver does not announce the group as a list of four items.
  evidence: The non-frozen Code Map specifies rows as `accessibilityRole="button"` + `accessibilityLabel`; the container has no role. `accessibilityRole="list"`/`"listitem"` macOS support is not verified in this story's API anchors. Additive a11y enhancement, out of scope for the frozen ACs; defer to an a11y-polish pass with on-device role verification.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sidebar-navigation-and-status-header-shell.md`
  summary: The `ref.focus()` wiring (mount-focus on row 0 and focus-on-select) is not unit-tested — removing the focus calls leaves every test green, so a broken ref chain or dropped focus call ships undetected.
  evidence: In react-test-renderer, refs to host `View` components are not populated (`rowRefs[i].current` is `null` in the node jest env), so `rowRefs[i].current?.focus()` is a no-op and cannot be spied without mocking the ref mechanism itself. The Matrix Test Audit documents this as a native-runtime behavior covered by code review + the optional on-device manual check (which verifies the visible focus ring). The gap is a genuine test-env limitation; defer — revisit if a feasible spy approach (e.g. mocking `useRef`/the host ref) is warranted, and rely on the manual check in the meantime.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sidebar-navigation-and-status-header-shell.md`
  summary: `StatusHeader` is fully static but re-renders on every surface change; `selectRow`/`onKeyDown` in `Shell` are fresh closures each render (not `useCallback`-memoized).
  evidence: `Shell` holds `useState` and re-renders on every navigation; `StatusHeader` has no props but no `React.memo`, so it re-renders trivially each time. For a 4-row placeholder shell the render cost is negligible and adding `React.memo`/`useCallback` now is speculative (the same "simplest correct choice" reasoning the spec used to reject Zustand). Defer to a perf pass once the shell carries real state churn (Epic 2/4).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sidebar-navigation-and-status-header-shell.md`
  summary: `SidebarRow` labels have no `numberOfLines`/`ellipsizeMode`, so a future longer or localized label would wrap to two lines and break the row height.
  evidence: The four current labels ("Blocklist"/"Timer"/"Schedule"/"Settings") are short and won't wrap; the frozen Boundaries fix the row labels. `numberOfLines={1}` + an ellipsize mode is a cheap safeguard for future localization/longer labels, but there is no current consumer. Defer to an i18n/l10n pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sidebar-navigation-and-status-header-shell.md`
  summary: `SidebarRow` exposes `accessibilityRole="button"` + `accessibilityLabel` but no `accessibilityHint` describing what activating the row does (changes the surface + moves focus).
  evidence: The non-frozen Code Map specifies `accessibilityRole="button"` + `accessibilityLabel` only; `accessibilityHint` is an additive a11y enhancement not in the frozen ACs. Defer to an a11y-polish pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-sidebar-navigation-and-status-header-shell.md`
  summary: `__tests__/App.test.tsx` is a no-assertion smoke test; it does not verify that `App` renders `<Shell/>`, so a broken App→Shell rewire (e.g. an accidental revert to a placeholder body) ships with a green suite.
  evidence: `App.test.tsx` calls `ReactTestRenderer.create(<App />)` and makes zero assertions. Strengthening it to assert the Shell subtree (sidebar rows / status header text) requires mocking `react-native-safe-area-context`: App's `SafeAreaProvider` is rendered without `initialSafeAreaInsets`, and in the node jest env the provider renders null children while insets are null (per the spec's test-quirk notes — `Shell.test.tsx` passes `initialSafeAreaInsets` for exactly this reason), so the Shell subtree does not mount under `<App />` in the test env. `initialSafeAreaInsets` is a test-only prop that cannot be added to production `App`. Defer to a test-strengthening pass that mocks the safe-area module (or `useSafeAreaInsets`) for `App.test.tsx`; `Shell.test.tsx` already covers Shell directly and the App→Shell wire is a one-line JSX change, so the risk is low until then.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-configstore-turbomodule-and-config-shape.md`
  summary: `config.json` and its `Frosthalt` directory are created with default permissions (file 0644, dir 0755 via umask) but will hold a salt-free SHA-256 `passwordHash` (AD-9) from Epic 3; the file/dir should be hardened to 0600/0700 so the credential-at-rest is not world/group-readable.
  evidence: `ConfigStore.swift` uses `FileManager.createDirectory(at:withIntermediateDirectories:)` (default 0755) and `Data.write(to:options:[.atomic])` (default 0644). AD-9 stores a salt-free SHA-256 `passwordHash` in this same `config.json`. Story 1.4 populates no `passwordHash` (the field is typed but unset; only `domains`/`settings` are written this epic), so the risk is latent in 1.4 and becomes real in Epic 3 (story 3-1) when the password hash is first written. The frozen 1.4 spec does not require file-permission hardening (no AC or Boundary pins permissions), so this is not a 1.4 deviation. Defer to Epic 3 — when `passwordHash` is populated, set `FileManager.FileAttributeKey.posixPermissions = 0o600` on the atomic write and `0o700` on the directory create (or re-chmod on first write). Surfaced incidentally by the step-04 review.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-shellrunner-turbomodule-and-hosts-file-contract.md`
  summary: Two concurrent `writeHosts` calls race on `/etc/hosts.fh.bak` and `/etc/hosts.new` and could corrupt `/etc/hosts`; there is no serialisation in 1.5.
  evidence: `ShellRunner.writeHosts` dispatches each call to a single `backgroundQueue` (serial), so calls do not run concurrently with each other — but the spec's "Never" list explicitly assigns the Apply-pipeline serialisation/queue to 1.6, and the 1.6 domain layer is the SOLE allowed caller (AD-5). 1.5 has no JS caller (only the Jest-mocked test), so the race is not reachable in 1.5; it becomes the 1.6 Apply pipeline's job to serialise Apply invocations. The marker-scan vs `cp` TOCTOU is the same concurrency concern. Defer the serialisation design to 1.6; the serial `backgroundQueue` is an incidental partial mitigation, not the spec'd serialiser.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-shellrunner-turbomodule-and-hosts-file-contract.md`
  summary: `/etc/hosts.fh.bak` is never cleaned up or rotated, uses a fixed name, and is world-readable (0644); it persists across reboots and is overwritten each Apply.
  evidence: The frozen "Always" batch step 1 is `cp /etc/hosts /etc/hosts.fh.bak` — the name and the backup step are frozen, so changing the name or removing the backup requires human renegotiation of the frozen block. `/etc/hosts` is itself 0644, so a 0644 `.bak` leaks nothing not already world-readable. A concurrent unrelated root process using the same fixed name could collide, but that requires another root actor writing `/etc/hosts.fh.bak` simultaneously (rare). Defer: decide a retention/cleanup + uniqueness policy at the product level (e.g. delete-on-success vs keep-last-known-good, and `mktemp`-style name) — the latter needs a frozen-spec revision.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-shellrunner-turbomodule-and-hosts-file-contract.md`
  summary: The strict hosts-line regex `^(0\.0\.0\.0|::)\s+[a-z0-9]([a-z0-9.-]*[a-z0-9])?$` permits invalid DNS labels (consecutive dots like `example..com`, no 63-char per-label limit, no 253-char total limit).
  evidence: The regex is quoted verbatim in the frozen "Always" injection-safety bullet, so tightening it is a frozen-spec change (needs human renegotiation). 1.6's `normaliseDomain` produces the lines upstream, so in practice malformed labels never reach this gate; the native regex is defence-in-depth layer 1, not the primary hostname validator. Low impact. Defer — optionally fold a tightened regex (no consecutive dots, label/total length limits) into a future frozen-spec revision if hardening the native gate becomes a priority.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-shellrunner-turbomodule-and-hosts-file-contract.md`
  summary: `WriteResult` is exported from both `NativeShellRunnerSpec.ts` and the `shellRunner.ts` port, so nothing structurally prevents 1.6 code from importing the type directly from the spec and bypassing the port (weakens the AD-5 "sole import surface" intent).
  evidence: `shellRunner.ts` re-exports `WriteResult` "so the 1.6 domain layer imports it from the port", but the spec also exports it. Story 1.4's `NativeConfigStoreSpec`/`configStore` follow the identical pattern, so changing 1.5 alone would create inconsistency. Real enforcement needs an architecture-wide eslint `no-restricted-imports` rule gating `src/native/specs/*` to only `src/hosts/*` / `src/config/*`. Defer to a cross-story tooling/architecture pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-shellrunner-turbomodule-and-hosts-file-contract.md`
  summary: If the privileged `/bin/sh` is killed (SIGKILL/shutdown) mid-splice, a root-owned partial `/etc/hosts.new` could be left in `/etc` (cleaned by the EXIT trap only on a clean exit) and the app-owned `LINES_FILE` could be left in `/tmp`.
  evidence: The static body has `trap 'rm -f /etc/hosts.new' 0`, but SIGKILL bypasses traps, so a hard kill can orphan `/etc/hosts.new` (root-owned, partial). The `LINES_FILE` is app-owned (0600) and removed by Swift after the run; a kill of the RN app before that cleanup leaves it in `/tmp` containing the user's own blocklist (low exposure — it is the user's own data). `/etc/hosts` itself is never mutated until the atomic `mv`, so the live file stays consistent regardless. Defer — the residual-artifact risk under SIGKILL is low and bounded; revisit if a startup sweep of stale `/etc/hosts.new`/`fh_lines.*` is wanted.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-shellrunner-turbomodule-and-hosts-file-contract.md`
  summary: `runOsaScript` has no watchdog timeout on `proc.waitUntilExit()`; a hung osascript (admin prompt never answered, or a stuck subprocess) blocks the serial `backgroundQueue` indefinitely.
  evidence: `waitUntilExit()` blocks with no timeout. The call is async on `backgroundQueue` so the UI does not block (NFR-8 holds), but a hung call blocks subsequent `writeHosts` calls and pins the stderr-drain thread. Adding a timeout is a product decision: the admin-password prompt can legitimately take minutes, so a too-short timeout would abort a legitimate Apply, and `terminate()`-ing osascript mid-prompt has undefined UX. Defer — design a generous/abortable timeout policy (or rely on the 1.6 Apply pipeline's cancellation path) rather than bolt one onto the privileged adapter now.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-shellrunner-turbomodule-and-hosts-file-contract.md`
  summary: `/etc/hosts.new` uses a fixed name; a concurrent unrelated root process (or a second Frosthalt instance) writing the same path would collide.
  evidence: The serial `backgroundQueue` prevents self-collision within one app instance, and the EXIT trap removes a partial `/etc/hosts.new`, so the risk is limited to an external root actor using that exact name simultaneously (rare). The `/etc/hosts.fh.bak` name is frozen, but `/etc/hosts.new` is an implementation detail (not in the frozen spec), so it could be made `mktemp`-style unique inside the privileged body. Defer — low gain given the trap cleanup and the rarity; revisit alongside the `.bak` uniqueness/retention decision above.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-staged-then-apply-serialized-pipeline-proven-on-one-domain.md`
  summary: `runApply` is handed the call-time `committed` snapshot, so the strict-order config write bases non-domain fields (settings/schedules) on the state at Apply-click time, not at run time — a latent clobber once a second config-writer (settings/schedules UI) lands and mutates those fields while an Apply is queued.
  evidence: `store.ts` `apply()` captures `committedSnapshot = get().committed` at call time and passes it to `runApply`, which writes `{ ...committed, domains: staged }`. The success handler separately updates in-memory `committed` from the CURRENT `s.committed` (`{ ...s.committed, domains: stagedSnapshot }`), so the in-memory state is fine — only the persisted `config.json` write uses the stale snapshot. In 1.6 this is harmless: the ONLY config writer is Apply (no settings/schedules UI exists yet, so no non-domain field can change between an Apply's call time and its run time). It becomes a real clobber when a later epic introduces a direct (non-Apply) writer for `settings.menuBarEnabled` / `schedules` and a user mutates it while an Apply is queued behind an in-flight run — the queued Apply would write the stale `settings`/`schedules` and overwrite the direct change. Trivial to future-proof (`committed: get().committed` at run time inside the enqueued callback, keeping `stagedSnapshot` at call time — a 1-line change with no 1.6 behaviour shift), but it touches the serialization-critical path and has no trigger in 1.6, so it belongs to the story that introduces the second writer (settings UI / schedules). Defer to that story, with a test that stages a domain, starts a slow Apply, toggles `menuBarEnabled` mid-flight, and asserts the persisted config keeps the toggle.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-staged-then-apply-serialized-pipeline-proven-on-one-domain.md`
  summary: `applyStatus` flips back to `'idle'` for a microtask window between two queued Apply runs (run1's success/failure branch sets idle; run2's callback sets running only once it starts), so the Apply button briefly re-enables mid-queue and a fast user can fire an extra Apply that just re-queues.
  evidence: `applyStatus` is set to `'running'` inside the enqueued callback and reset to `'idle'` in the success/failure branches. Between run1 settling (idle) and run2's callback reaching its `set({applyStatus:'running'})`, there is a microtask gap where `applyStatus === 'idle'` despite a queued run pending. The button's `disabled={applyStatus === 'running'}` therefore re-enables for that gap. Not a correctness bug — the extra Apply is serialized like any other and runs after the queue drains — but it is a UI invariant gap ("disable Apply while in flight") if "in flight" is read to include "queued". The microtask window is negligible in practice. A proper fix needs queue-depth-aware `applyStatus` (set running at enqueue time when the queue is non-empty; reset to idle only when the queue is fully drained), which is more machinery than 1.6's "one at a time" proof warrants. Defer to a UI-polish/serialization pass once the Apply button carries real state churn (Epic 2).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-drift-detection-and-user-initiated-restore-section.md`
  summary: The `ok:false` envelope's `error` field is forwarded verbatim by every port (shellRunner readHostsSection/writeHosts, ConfigStore) without a `typeof error === 'string'` guard, so a native regression returning a non-string `error` (number/object) would leak into `lastResult.error` and the UI "failed: …" line.
  evidence: `shellRunner.ts` shape-guards `ok` (boolean) and, on `ok:true`, `section` (null|string[]), but on `ok:false` it returns the envelope as-is with no `error`-type check; `writeHosts` (1.5) and `ConfigStore` share the identical pattern, and 1.7's `readHostsSection` was explicitly specced to "mirror `writeHosts`'s `bad-envelope` guard" — so the omission is a pre-existing, cross-port pattern, not a 1.7 deviation. The native side always produces string error codes today, so the consequence is cosmetic (a "failed: [object Object]" line) and non-crashing. Defer to a focused "harden all port envelope guards" pass that string-validates `error` across the three ports together (low gain, low risk, but not 1.7's job).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-drift-detection-and-user-initiated-restore-section.md`
  summary: `restoreSection` (and `apply`) set `applyStatus:'running'` inside the enqueued callback, not at call time, so a fast double-click before the first callback runs enqueues two runs back-to-back — two sequential admin prompts (serialized, no corruption, but a double-prompt UX wart).
  evidence: The Restore/Apply button disables on `running || …`, but `running` only becomes true once the enqueued callback runs (next microtask), leaving a call-time→callback window where the button is still enabled. This is the SAME root as the existing 1.6 `applyStatus`-microtask-window entry above (both are facets of "`applyStatus` is queue-depth-unaware"); the single proper fix — set `applyStatus` at enqueue time based on queue depth and reset only when the queue fully drains — closes both windows at once. The serialization test already proves the two runs don't overlap; the only cost is an extra admin prompt the user can cancel. Defer to the same UI-polish/serialization pass that handles the sibling 1.6 entry (Epic 2), keeping the fix coherent across Apply + Restore rather than a one-off in 1.7.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-drift-detection-and-user-initiated-restore-section.md`
  summary: There is no native XCTest target in the repo, so the Swift `readHostsSection` marker-extraction logic (clean-pair triage, first-end-after-first-begin anchoring, body slice `Array(lines[(b+1)..<e])`, `omittingEmptySubsequences:false`) — like `writeHosts`'s Swift pre-scan — is exercised only through JS mocks that feed canned envelopes, never by a test that runs the Swift code.
  evidence: `find macos -name '*.swift' | xargs grep -l XCTest` returns nothing; `package.json` `test` is jest-only. A Swift off-by-one (e.g. `Array(lines[b..<e])` including the `# BEGIN FROSTHALT` marker line in the body) would make `computeDrift` always report `mismatch` for an actually-in-sync hosts, and every JS test would still pass (they mock `readHostsSection` or construct `ReadSectionResult` literals). The spec deliberately scoped native verification to `pnpm macos` (build) + manual live checks (its Verification section), so this is a spec-accepted, pre-existing limitation — 1.5's `writeHosts` Swift is equally untested natively. Defer to a dedicated "establish a native XCTest harness" effort (would cover both `readHostsSection` and `writeHosts` Swift against fixture `/etc/hosts` strings); not introduced by 1.7, only exposed by it adding more Swift.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-blocklist-surface-with-always-on-checkbox.md`
  summary: Double VoiceOver announcement on entry to surface 0 — Shell.selectRow speaks the hardcoded "Blocklist, 0 domains" (frozen Never clause, 2.5's scope) and then the mounted <Blocklist/> speaks "Blocklist, N domains, M always-on" (2.1 AC), so a VoiceOver user hears two back-to-back announcements on ⌘1/click.
  evidence: Both announces are mandated/protected by the spec (the AC requires the Blocklist entry announce; the frozen Never clause protects Shell's hardcoded announce until 2.5). The spec Design Notes flagged this tension at planning and chose to carry it to review. Resolving it needs the human to lift the frozen clause (drop/replace Shell's announce now) — deferred to 2.5, which owns the Shell-side count and will unify the two announces into one count-aware announce. Low severity (cosmetic duplication, not a correctness issue); no patch possible without touching the frozen block.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-blocklist-surface-with-always-on-checkbox.md`
  summary: The Blocklist rows container is a plain `View` (no `ScrollView`/`FlatList`), so a blocklist taller than the viewport clips with no scroll affordance.
  evidence: `Blocklist.tsx` renders `domains.map(...)` into `<View style={styles.list}>` with `flex:1`; there is no scroll wrapper and no story-level cap on domain count. 2.1 has few domains so it is not hit in practice, but the surface has no overflow handling and no test for it. The 2.1 spec did not require scroll (out of scope); defer to a polish pass or the story that first expects a long list.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-blocklist-surface-with-always-on-checkbox.md`
  summary: The Blocklist surface renders no apply-failure/status feedback — it subscribes to `applyStatus` only to gate buttons and never reads `lastResult`, so an admin-denied Apply or config-write failure leaves the checkbox toggled (staged retained) with no visible explanation in the surface.
  evidence: `Blocklist.tsx` reads `applyStatus` but not `lastResult`; the store comments say "the UI status line reflects the denial," and that status line lives in the StatusHeader — explicitly out of scope for 2.1 (frozen Never clause: leave StatusHeader alone, 2.5's scope). Defer to 2.5, which owns the status-header denial/status line.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-blocklist-surface-with-always-on-checkbox.md`
  summary: No visual "running" indicator on Apply — when `applyStatus==='running'` the Apply button is disabled and dims to opacity 0.4, indistinguishable from the clean/no-staged disabled state, with no spinner/label change/"Applying…" state.
  evidence: `ApplyButton` is visual-only (no `running` prop per the 1.2 primitive); `Blocklist` passes `disabled={running || !hasStaged}`. The osascript prompt may be off-screen, so the user cannot tell an in-flight Apply from a clean-disabled Apply. The 2.1 spec Design Note explicitly assigns the Apply pulse/"N changes staged" hint to 2.3; defer to 2.3.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-blocklist-surface-with-always-on-checkbox.md`
  summary: `stageAlwaysOnToggle` does not normalise its `hostname` argument (raw `hostname === d.hostname` compare), an asymmetry with `stageDomainAdd` which normalises via `normaliseDomain(raw)` — a future caller passing a non-normalised form (e.g. "Example.com") silently gets `{ok:false,error:'not-found'}`.
  evidence: `store.ts` `stageAlwaysOnToggle` does `base.findIndex((d) => d.hostname === hostname)` with no normalisation; current callers pass `domain.hostname` (already the stored apex) so it works, but the precondition is undocumented and the asymmetry is a trap. Not a current bug (no caller passes a raw form); defer — either normalise here too or document the "must be the stored apex" precondition.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-blocklist-surface-with-always-on-checkbox.md`
  summary: `draftEqualsCommitted` compares only `hostname` + `alwaysOn` (correct today since `Domain` has exactly those fields), but the generic name hides the field scope — if `Domain` ever gains a field, clean-revert would incorrectly fire when that field differs from committed, silently dropping the edit.
  evidence: `store.ts` `draftEqualsCommitted` loops `hostname`/`alwaysOn` only. `Domain` is `{hostname, alwaysOn}` today (config shape, epic-relevant), so it is correct; the latent trap is the name implying a full deep-equal. Defer — either deep-compare or rename to make the field-scope explicit when `Domain` gains a field.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-blocklist-surface-with-always-on-checkbox.md`
  summary: Duplicate-hostname edge cases are undefended: `stageAlwaysOnToggle`'s `.map` flips EVERY matching domain (not just one), and `Blocklist` renders rows with `key={d.hostname}` — both misbehave if `committed.domains` ever contains duplicate hostnames.
  evidence: The config invariant is that `hostname` is the PK, deduped at add by `stageDomainAdd`; no current store action produces duplicates, so this is unreachable through the UI. A hand-edited corrupt `config.json` (ConfigStore returns it as-is if shape-valid) could contain dupes, making toggles ambiguous and React keys collide. Pre-existing invariant gap, not caused by 2.1's reachable behavior; defer to a config-integrity/validation pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-blocklist-surface-with-always-on-checkbox.md`
  summary: `DomainRow` puts `accessibilityRole="text"` on a focusable `View` wrapper (non-idiomatic for RN macOS); the hostname is read via `accessibilityLabel`, not the role, so the role's macOS NSAccessibility mapping is unverified.
  evidence: `DomainRow.tsx` sets `accessibilityRole="text"` + `accessibilityLabel={domain.hostname}` on the focusable `View`; `accessibilityRole="text"` is idiomatic on `Text`, not a `View`. It works today (label is spoken, Tab order checkbox→domain is asserted), but the role semantics on a focusable View are non-standard. Defer — revisit after native VoiceOver verification; low risk to change but no native a11y test to confirm the replacement is better.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-blocklist-surface-with-always-on-checkbox.md`
  summary: No list-level a11y structure — the rows container has no `accessibilityRole="list"`, rows have no `accessibilityRole="row"`, and the surface has no region landmark, so beyond the one-shot mount announce VoiceOver region/element navigation gets no "Blocklist" landmark or row semantics.
  evidence: `Blocklist.tsx` renders `<View style={styles.list}>` and `<View style={styles.container}>` with no accessibility roles; only the mount announce + per-checkbox/per-label stops exist. Enhancement beyond the 2.1 AC (which requires the mount announce + Tab order + checkbox roles, all satisfied); defer to an a11y-polish pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-blocklist-surface-with-always-on-checkbox.md`
  summary: No surface-level end-to-end test of the toggle→Apply→committed loop — `Blocklist.test.tsx` asserts pressing a checkbox calls `stageAlwaysOnToggle` and pressing Apply calls `apply()` as independent wiring checks, but no test renders Blocklist, presses a checkbox, presses Apply, awaits the (mocked) apply, and asserts the row now reflects the toggled `alwaysOn` from `committed` and `staged` cleared.
  evidence: The two halves are tested separately (toggle wiring at `Blocklist.test.tsx`, apply wiring + store apply-commit in `store.test.ts`); the optimistic-then-commit loop — the whole point of the surface — is not exercised end-to-end at the surface layer. The verification-gap reviewer confirmed matrix rows 1–7 are each covered, so this is a stronger-test gap, not a missing-coverage gap. Defer — driving the async serialized `apply()` through to a committed state in a component test is higher-cost; the wiring + store logic are already proven.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-add-domain-field-with-live-normalisation.md`
  summary: AddDomain's UI duplicate check uses `staged ?? committed.domains`; when 2-4 adds domain removal, a committed domain absent from the current draft could be re-staged without an "Already in your list." error.
  evidence: The UI gate and `stageDomainAdd` both check duplicates against `staged ?? committed.domains`, so they agree today — but only because 2-2 has no remove (staged is always committed + adds/toggles). Once 2-4 can drop a domain from the draft, a committed-but-removed domain is no longer in `staged`, so re-adding it would bypass the duplicate error while it is still live in config. Revisit at 2-4: union committed+staged for the duplicate check, or scope the copy ("Already in this draft" vs "Already in your list").

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-add-domain-field-with-live-normalisation.md`
  summary: AddDomain's `handleAdd` silently swallows a `stageDomainAdd` `{ ok: false }` result (no inline error, field not cleared) — unreachable today, but no fallback error path exists.
  evidence: `handleAdd` only branches on `result.ok === true` (clears the field) and does nothing on `ok: false`. The UI gate (`addEnabled` requires `normalised != null && !isDuplicate`) prevents the only `ok:false` cause (`invalid-domain`), and duplicates return `{ok:true}` idempotently, so the `ok:false` path is unreachable in 2-2. When a real `ok:false` cause appears (e.g. 2-3 apply-failure surfacing, or future store errors), define a fallback inline error message and surface it on the `ok:false` branch instead of silently leaving the field unchanged.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-effective-blocklist-computation-and-apply-integration.md`
  summary: `stagedChangeCount` is order-agnostic (a `Map` by hostname) while its sibling `draftEqualsCommitted` (`store.ts:282`) is order-sensitive (index-by-index) — the two disagree on a reordered-but-value-equal pair, so once 2-4 adds remove+re-add a net-zero reordered draft yields `count === 0` and the hint reads "0 changes staged" with a pulsing Apply.
  evidence: `stagedChangeCount` (`stagedChangeCount.ts`) walks a `Map<hostname, alwaysOn>` and counts added/removed/toggled, so a reordered value-equal `staged` vs `committed` returns 0. `draftEqualsCommitted` (`store.ts:282-289`) compares `hostname`+`alwaysOn` pairwise in order, so the SAME reordered pair returns `false` (not equal) — the two helpers are inconsistent. In 2-3 the inconsistency is unreachable: `stageDomainAdd` appends (no reorder) and `stageAlwaysOnToggle` `.map`s in place (no reorder), so `staged` is always `committed`-order-plus-tail, and the clean-revert invariant `staged != null ⟹ stagedChangeCount >= 1` holds. The 2-3 spec/Code-Map claim that the helper "matches `draftEqualsCommitted`'s discipline" is slightly inaccurate (it matches the per-hostname-diff discipline, not the order-sensitivity). It breaks in 2-4, where remove+re-add can produce a reordered value-equal draft: `draftEqualsCommitted` returns `false` (so `staged` is NOT cleared to null) while `stagedChangeCount` returns 0 — yielding "0 changes staged" + a pulsing Apply button on a net-zero draft. Defer to 2-4: either make `draftEqualsCommitted` order-agnostic (hostname-set equality, so the clean-revert fires and `staged` clears) OR guard the hint/pulse against `changeCount === 0`. The clean-revert path is the cleaner fix (aligns the two siblings and preserves the `staged != null ⟹ count >= 1` invariant); add a 2-4 test for the reordered-value-equal draft.
  related: the 2-1 deferred item on `draftEqualsCommitted`'s field scope (`hostname`+`alwaysOn` only) — both are latent traps in the same module-private helper, best fixed together at 2-4.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-effective-blocklist-computation-and-apply-integration.md`
  summary: AC8's ports-boundary ("no `child_process`/`fs`/`os` in `src/`") has no automated test in any story — every story re-verifies it by manual grep at review, so a stray privileged import could ship green.
  evidence: AC8 (spec line 98) requires domain-layer code to import ShellRunner/ConfigStore only via the ports and never `child_process`/`fs`/`os`. The 2-3 verification-gap reviewer confirmed the new `stagedChangeCount.ts` is clean, but the AC is re-checked by hand each story (a `grep -rn "require('child_process')\|from 'fs'\|from 'os'" src/`), not by a failing test or lint. This is a cross-story/cross-cutting gap (true for every story since 1.5), not a 2-3 deviation. The right fix is a repo-wide ESLint `no-restricted-imports` rule banning `child_process`/`fs`/`os` (and the `node:` forms) within `src/`, which would fail the build on a stray import. This is the sibling of the existing 1-5 deferred item (an `no-restricted-imports` rule gating `src/native/specs/*` to only the port modules); both are the same "enforce the ports-boundary architecturally, not by hand" pass, best done together so the boundary has one lint config. Defer to that cross-story tooling pass; not 2-3's job.
  related: the 1-5 deferred item on `WriteResult` import surface / `no-restricted-imports` for `src/native/specs/*` — same lint-config pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-6-read-only-hosts-viewer-with-drift-warning.md`
  summary: `apply()` success updates `committed`/`staged`/`lastResult` but does NOT re-read hosts / refresh `drift` + `lastReadSection`, while `restoreSection()` success does — so after a successful Apply, `drift` is stale until the next `checkDrift()`.
  evidence: `store.ts:274-279` (the `runApply` success branch) sets `committed`/`staged`/`applyStatus`/`lastResult` but not `drift`/`lastReadSection`; `restoreSection()` success (`store.ts:329-336`) re-reads via `readHostsSection()` + `computeDrift` and sets both. Both write `/etc/hosts`, so the asymmetry means Apply leaves `drift` potentially reading `missing`/`mismatch` until a later `checkDrift`. This predates 2.6 (Apply never refreshed drift; 2.6 didn't touch `apply()`), and 2.6 surfaced it incidentally by making `lastReadSection` load-bearing. No current user-visible impact: the only persistent drift consumer is the read-only viewer (2.6), which calls `checkDrift()` on mount and so self-refreshes on open; the Blocklist surface shows no drift. The gap becomes real the moment a persistent drift surface (e.g. a status-header drift indicator or a Blocklist drift banner) is added. Defer — re-checking drift in `apply()`'s success branch (reusing the existing `checkDrift` action, no new port call) closes the asymmetry, but is out of 2.6's frozen scope (which added the viewer only) and has no current impact. Revisit when a persistent drift surface lands, or as a small store-hygiene pass alongside 1.7's drift model.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-set-password-salt-free-sha-256.md`
  summary: Story 3-2's password gate sheet MUST apply the same trim-before-hash convention as 3-1's `setPassword` (which hashes `entry.trim()`), else a password containing leading/trailing spaces produces a different hash at gate time than at set time and locks the user out of their own gated action.
  evidence: `SetPassword.tsx` passes `entryTrimmed` (=`entry.trim()`) to `setPassword`, so `passwordHash = sha256(trimmed)`; the gate sheet in 3-2 re-hashes the user's entry and compares to `committed.passwordHash`. If the gate sheet hashes the raw (untrimmed) entry, `sha256(" secret123 ") !== sha256("secret123")` → every gate entry for a spaced password is rejected. The trim convention is 3-1's implementer choice (the spec's matrix says `sha256(entry)` and "blank → no error spam", which trim reconciles); it is not yet captured in 3-2's spec. Real cross-story consistency risk; defer to 3-2's planning so its gate sheet trims identically (or, alternatively, 3-1 drops the trim and both hash raw input — a design call for 3-2 planning).
  resolved_by: Story 3-2 step-04 review patch — `PasswordGate.tsx` `handleSubmit` now trims `entry` before `verifyPassword` (matching `SetPassword`'s trim-at-submit), locked by `__tests__/PasswordGate.test.tsx` "a correct entry with accidental surrounding spaces still verifies".

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-set-password-salt-free-sha-256.md`
  summary: An unanswered OS admin prompt (osascript) hangs the serialized `enqueue` queue indefinitely, and `setPassword` — now sequenced through that same queue — inherits the hang: its returned promise never settles and the SetPassword UI stays on "Saving…" forever.
  evidence: `setPassword` routes through the shared `enqueue` (`store.ts` `setPassword`), the same queue `apply()`/`restoreSection()` use. `writeHosts` awaits an osascript admin prompt; if the user walks away from it, the running Apply job never resolves, so the queued `setPassword` job never runs and its promise never settles. This is a pre-existing Apply-pipeline behaviour (admin prompt can already hang Apply itself + any later Apply); 3-1 surfaces it incidentally by joining the queue. Not 3-1's to fix (the spec marks `apply.ts` read-only and the queue is Epic 1's). Defer to a focused Apply-pipeline pass: consider a queue-level timeout/abort, or at minimum a UI "cancel" for a hung admin prompt. Note 3-1's `SetPassword` `.catch` only covers rejections, not a never-settling promise, so it does not address this.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-password-gate-sheet-reusable.md`
  summary: `requirePassword` while the gate is already open silently overwrites the in-flight `gateAction` (the first caller's action is dropped); the gate is modal with no defined double-open policy.
  evidence: `store.ts` `requirePassword` unconditionally `set({ gateOpen: true, gateAction: action })` even when `gateOpen` is already true, so a second caller displaces the first action the user is mid-verifying for. Unreachable in v1 — every caller (3-3 change-password, 3-4 Panic, 4-6 end-early) is user-initiated and sequential, so two `requirePassword` calls never overlap — but it is a silent-data-drop in the reusable mechanism. Not patched in 3-2 because the naive guard (`if (gateOpen) return`) conflicts with the re-open-from-action path that 3-2's `runGateAction` identity-compare close-guard enables (a caller whose action synchronously re-enters `requirePassword` must NOT be blocked). Needs a design call: no-op (preserve first) vs queue vs replace, plus a flag to distinguish "inside an action" from "external second caller". Defer to the story that first has overlapping callers (or a focused gate-mechanism pass); add a test for whichever policy is chosen.
  related: 3-2's `runGateAction` re-open identity-compare guard (Shell.tsx) — the two behaviours must be designed together.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-password-gate-sheet-reusable.md`
  summary: The gate scrim has no `onPress`/click-outside-to-dismiss affordance; a click in the dim area outside the panel does nothing (the sheet can only be closed via Cancel/Esc).
  evidence: `PasswordGate.tsx` renders the scrim `<View>` with no `onPress`/`pointerEvents`; only the Cancel button + Shell Esc branch close the gate. This is arguably CORRECT for a password gate (a stray click should not abort a verify-in-progress the user is mid-typing), and `HostsViewer` (the mirrored overlay) was not audited for the same affordance, so a product decision is needed before adding it. The 3-2 frozen block does not require scrim-dismiss. Defer to a product/UX pass that decides scrim-dismiss for BOTH the gate and the hosts viewer consistently (and, if yes for the gate, whether it should confirm or just cancel).

- source_spec: `_bmad-output/implementation-artifacts/spec-3-4-panic-unblock-password-gated.md`
  summary: Panic unmount-during-apply race loses the success toast. If the user navigates away from Settings (or unmounts Panic) between the successful `apply()` resolving and the success toast rendering, the late `.then` still calls `setToastVisible(true)` on the unmounted component — React silently swallows it, but the user never sees the "All blocks cleared." / "Re-enable your blocklist" confirmation. The fix needs Shell-level navigation + state coordination (the success-toast is component-local today), so it is a meaningful UX re-architecture, not a one-line patch.
  evidence: The bug is theoretical for v1 (the user is on Settings when they tap Confirm; navigating away mid-apply requires ⌘1-⌘4 with the gate closed, which the surface routing allows but is unusual UX). The apply pipeline has no `applyStatus === 'success'` snapshot the Shell could read, and the toast is intentionally component-local per the spec's Code Map note. The P2 stale-resolve guard (`confirmTokenRef`) protects against Cancel + late-apply, not unmount-during-apply. Defer to a future Shell-level toast infra pass (the same one the spec's Code Map notes already defer), where the toast can survive Panic's unmount by living in Shell.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-live-countdown-hybrid-ring-scoped-slice.md`
  summary: The new native `react-native-svg` dependency is verified only at the JS layer — no automated check confirms the RNSVG pod is compiled/linked into the built macOS app. A green jest + tsc run is possible with a stale binary where the Timer Blocked path throws the moment CountdownRing renders.
  evidence: Jest renders only view-type strings (`RNSVGSvgView`/`RNSVGCircle`) without loading the native module; jest.config.js has no build/e2e step and package.json scripts have none either. Podfile.lock contains RNSVG but nothing automated pins the built app. Requires a manual `pod install` + macOS build/launch smoke test of the Blocked surface (already on the user-side manual checklist for this story).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-live-countdown-hybrid-ring-scoped-slice.md`
  summary: `readConfig` validates only top-level keys and not element shapes, so a hand-edited `config.json` can carry `activeTimer.endEpochMs` as a string/NaN. Story 4-3's defensive guards (timerStore non-finite park, Timer's usable-end gate) contain the fallout, but the boundary itself stays unchecked.
  evidence: `src/config/configStore.ts:73-87` documents that element shapes are deliberately not validated; the `ActiveTimer` type guarantees `endEpochMs: number` only at compile time. A future hardening pass should shape-check `activeTimer` (and domain elements) at the readConfig boundary rather than relying on each consumer's defensive normalization.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-live-countdown-hybrid-ring-scoped-slice.md`
  summary: `toLocaleTimeString([], { hour: '2-digit', minute: '2-digit', hour12: false })` can render "24:00" instead of "00:00" at local midnight on some ICU builds; `hourCycle: 'h23'` would be deterministic. NOT fixed because the exact derivation call is inside the spec's `<frozen-after-approval>` block.
  evidence: Known ICU behavior with `hour12: false` at the h24 boundary; only triggers when a session end lands exactly on midnight. The frozen block mandates the current call verbatim, so any change needs the human to renegotiate the frozen text first (cosmetic, rare).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-countdown-in-the-status-header.md`
  summary: After the last subscriber's `stop()`, the slice keeps its future-dated `endEpochMs`/`totalMs` (stop only clears the driver and parks `nowMs`), so `selectRemainingMs` keeps reading >0 with no session mirrored — a "ghost countdown" for any consumer that reads the selector without gating on its own `committed.activeTimer`.
  evidence: `timerStore.ts` `stop()` (:186-195) intentionally leaves `endEpochMs`/`totalMs` in place; `selectRemainingMs` (:73-74) derives purely from the slice. Every current consumer (Timer 4.3, StatusHeader 4.4) gates rendering on its own `activeTimer != null` normalisation, so nothing ghosts today — this is the slice's documented contract ("consumers gate rendering on their own activeTimer state, not on this selector"). Logged so 6.2's menu-bar subscriber does not miss the gate; a `stop()`-clears-mirror change would alter slice semantics (spec Ask-First boundary) for no current benefit.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-countdown-in-the-status-header.md`
  summary: The superseding-session re-arm claimed by both lifecycle comments ("Keyed on the mirrored endEpochMs so a superseding session re-arms the driver", StatusHeader.tsx + Timer.tsx) is untested at the component level — no test replaces a live `activeTimer` with a DIFFERENT live `endEpochMs` while the component stays mounted.
  evidence: Both Blind-Hunter and Verification-Gap reviewers flagged it; VerGap further established it is unreachable today — the only writer of a non-null `committed.activeTimer` is `stageStartTimer` (store.ts) and the Start control is pressable only when `!hasActiveTimer` (Timer.tsx). It becomes reachable in Story 4.7 (launch re-arm can write a new `activeTimer` over a live one). Defer the test to 4.7, where the path first executes; a keying regression (e.g. `[hasActiveTimer]`) would pass today's suite.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-countdown-in-the-status-header.md`
  summary: With the header now the always-first subscriber, a session resumed from persisted config at launch captures `totalMs = endEpochMs - Date.now()` (remaining only), so the header mini-ring starts FULL for an already half-elapsed session — user-visible for the first time through 4.4, owned by 4.7's launch re-arm.
  evidence: `timerStore.ts` `start()` captures `totalMs = Math.max(0, endEpochMs - Date.now())` at first start (:163-166); on relaunch that first start happens at launch time, not session-start time. The 4.3 slice design note called this sticky-`totalMs` behaviour intentional for remounts; the launch case is the 4.7 re-arm story. Documented in StatusHeader.tsx's doc comment (4-4 review patch 3c) so it is not rediscovered as a bug.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-countdown-in-the-status-header.md`
  summary: The identical slice-teardown sequence (`stop(); stop(); setState({ nowMs: 0, endEpochMs: null, totalMs: null })`) is duplicated in `__tests__/StatusHeader.test.tsx` and `__tests__/Shell.test.tsx` afterEach blocks; a single store-level `resetForTests()` helper would be robust, self-documenting, and reused by 6.2's suites.
  evidence: Both suites need it now that the header subscribes to the slice in Shell tests; the double-`stop` absorbs a leak of at most 2 refcount slots (the current ceiling — Shell tests leak at most 1, the StatusHeader co-subscriber test exactly 2). Pure test-hygiene refactor; defer to the story that next touches the slice (6.2) or a test-infra pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-countdown-in-the-status-header.md`
  summary: The header badge's amber "Blocking" ramp (EXPERIENCE.md's free → amber → blocked progression, incl. "schedule ending soon" → amber) is half-landed: 4.4 made the badge dynamic for the first time since 2.5 (free/blocked flip) but the amber step is neither built nor assigned to a story.
  evidence: EXPERIENCE.md:64/87 describes the amber header state; 4.4's frozen Never clause excludes expiry handling (4.5) and schedule work (Epic 5), and no existing deferred item covers the ramp. Product/design decision needed on when the badge turns amber (last N minutes of a session? schedule window proximity?) — defer to the story that first needs it (likely 4.5 expiry UX or Epic 5), not a 4-4 patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-countdown-in-the-status-header.md`
  summary: The header's ring `progress` for a mid-session mount (system clock already advanced N minutes before render) is never asserted — the sticky-`totalMs` behaviour is only exercised through the header from the exact-start position.
  evidence: The slice-level sticky-`totalMs` branch IS pinned in timerStore tests (prev.endEpochMs === endEpochMs reuses totalMs), and CountdownRing's progress math is unit-tested at 4.3; what is untested is the header passing a mid-session progress through. Asserting it requires mocking CountdownRing module-wide in StatusHeader.test.tsx to capture props (the real ring's SVG internals are invisible to the test renderer), which would rewrite the file's existing ring assertions — not worth it for a path that composes two tested halves. Revisit if 6.2 mirrors progress in the menu bar.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-auto-unblock-on-expiry-unless-also-always-on.md`
  summary: The `hosts-throw` catch branches never update `lastResult` — after an unexpected throw, `lastResult` keeps its previous (possibly success) envelope while `applyStatus` resets to idle; affects BOTH `expireTimer` (4.5) and the pre-existing `stageStartTimer` (4.2), whose catch shape `expireTimer` mirrors exactly.
  evidence: store.ts `expireTimer` catch sets `{applyStatus:'idle', toast}` only; the deny branch sets `lastResult: result`. `stageStartTimer`'s catch (4.2, review-patch 2) has the identical shape, so fixing only 4.5 would create an asymmetry between the two mirror actions. Consumers of `lastResult` are tolerate the staleness today; fix both catches together in a pass that touches the Apply-path invariants (a natural fit for 4.6, which reuses the expiry path).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-auto-unblock-on-expiry-unless-also-always-on.md`
  summary: Panic's component-local toast (Panic.tsx styles.toast) has border + padding but NO backgroundColor, same as the Shell toast before the 4-5 review patch added `tokens.monoBg` — the 3-4 toast text can float illegibly over scrolled content.
  evidence: Panic.tsx:530-540 — `toast` style carries only border/spacing/radius. The 4-5 Shell toast now sets `backgroundColor: tokens.monoBg` (review patch); the identical 1-line cosmetic fix in Panic was left out of 4-5 scope (Panic.tsx is not a 4-5 edit target and its toast shipped through the 3-4 review). Batch with any future Panic or Epic-5 toast-primitive work.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-6-end-early-password-gated.md`
  summary: `apply()` (:562-607) and `restoreSection()` (:625-663) lack the defensive hosts-throw try/catch that the three timer actions now have — if `writeHosts`/`writeConfig` threw unexpectedly inside those paths, `applyStatus` would stick at `running` with no envelope. Pre-existing gap, untouched by 4-6 (only the three timer actions were in scope for the lastResult fix).
  evidence: store.ts `apply` and `restoreSection` bodies run the hosts write without a wrapping try/catch, unlike `stageStartTimer`/`expireTimer`/`endEarly` whose catch branches now always reset `applyStatus: 'idle'`. No current code path makes the native port throw (configStore.ts:100-120 even envelopes writeConfig), so this is hardening, not a live bug. Fix together whenever the Apply-path invariants are next touched.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-schedule-surface-with-enable-toggle-and-summary.md`
  summary: `Shell.selectRow`'s nav announce hard-codes the "domains" count noun on every surface — pressing ⌘3 (Schedule) speaks "Schedule, N domains", where N is the effective-blocklist domain count. Wrong count noun on the Schedule surface (it has its own row count, announced separately by the Schedule surface's mount announce); pre-existing pattern from 2.5, untouched by 5-1.
  evidence: Shell.tsx `selectRow` — `AccessibilityInfo.announceForAccessibility(\`${SURFACE_NAMES[i]}, ${count} ${count === 1 ? 'domain' : 'domains'}\`)` where `count = effectiveBlocklist(committed).length`. Fixing properly needs a per-surface count-noun map (Timer/Settings surfaces also have no "domain" count) — a Shell-wide nav-announce rework, out of 5-1 scope. Natural fit whenever the Shell's nav announce is next touched (e.g. Epic 6 badge mirror work).

- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-schedule-editor-sheet.md`
  summary: A schedule's domain membership is invisible outside the editor sheet — `formatScheduleSummary` renders only days + times, so neither the Schedule surface rows nor the editor's live summary show which domains a schedule blocks; the user must open the editor and read checkbox states.
  evidence: scheduleSummary.ts `formatScheduleSummary` implements the frozen 5-1 summary grammar (days + times only); the 5-2 editor passes `domains: selectedDomains` into it and they are ignored. Showing domains would change 5-1's frozen summary grammar (a human renegotiation, not a 5-2 patch) — e.g. appending "· 3 domains" or a domain list to the row summary. Natural fit for an Epic 5 retrospective decision or whenever the Schedule surface summary is next redesigned.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-schedule-editor-sheet.md`
  summary: Committed schedules with legacy unpadded times (hand-edited `'9:00'` in config.json) are never normalised on read — they render raw in rows/summaries, ride `scheduleValueKey` unpadded, and one open→Save no-op edit would stage a padding-only change; 5.3's active-window evaluation must handle unpadded `HH:mm` too.
  evidence: normaliseTime pads only on Save (`stageScheduleUpsert`); nothing in the 5-2 diff normalises committed schedules at read time (configStore's shape gate validates the schedules ARRAY only). Pre-existing data-shape concern surfaced incidentally by 5-2's new parser — a read-time normalisation/migration pass is out of 5-2 scope. Hard requirement for 5.3: its window evaluation must not rely on zero-padded lexical comparison against legacy data (or must normalise committed schedules first).

- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-active-window-blocking-schedule-in-effective-blocklist.md`
  summary: A committed schedule missing `enabled` (hand-edited config.json) renders its row checkbox UNCHECKED on the Schedule surface (ScheduleRow reads the raw field) while the evaluator — and therefore actual enforcement — treats missing-enabled as ON. Behaviour is safe (blocking grows, never shrinks) and the 5.3 evaluator's coercion is spec-frozen, but the row's visual state disagrees with enforcement until the row is re-saved through the editor.
  evidence: ScheduleRow.tsx:94 reads `schedule.enabled` directly for the checkbox; the store normalises `enabled` only on editor round-trip (stageScheduleUpsert), never on read; configStore's shape gate validates the schedules ARRAY only. Fix belongs to the read-time normalisation family already deferred from 5-2 (unpadded committed times) — one store-level pass that normalises committed schedules (explicit `enabled` boolean + padded times) at readConfig time, with row render following automatically. Do not patch the row alone — it would re-derive the coercion in a second place.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-active-window-blocking-schedule-in-effective-blocklist.md`
  summary: The store-level hosts-payload paths other than Apply (restoreSection, timer start/expire/end-early) are not asserted to include active-schedule lines — their schedule inheritance is only proven indirectly, via the shared `effectiveHostsLines`/`effectiveBlocklist` unit tests and the Apply-level payload tests.
  evidence: store.test.ts's restore/timer tests pre-date schedules and assert call counts/envelopes, not payload lines; 5.3 added payload assertions only on `runApply`. All four paths call the same `effectiveHostsLines(committed)` helper whose schedule contribution is unit-pinned, so the gap is integration-coverage, not correctness. Natural fit: whenever those store tests are next touched (e.g. 5.4's ticker work, which exercises the timer paths against schedules for real).

- source_spec: `_bmad-output/implementation-artifacts/spec-5-4-live-schedule-transitions-while-app-running.md`
  summary: Midnight-crossing schedule windows (`startTime` 22:00, `endTime` 06:00) can never be active — the 5.3 evaluator treats `endTime <= startTime` as a degenerate window (never active), so 5.4's transitions/badge inherit that posture and a classic overnight block window simply never blocks. Surfaced by 5.4's review (the badge/transition code paths are correct; the limitation is upstream in `scheduleEval`).
  evidence: `scheduleEval.ts` `isScheduleActive` — `nowMinutes < endMinutes && nowMinutes >= startMinutes` style half-open check cannot represent a window spanning midnight; 5.3's frozen matrix pinned the degenerate-window contract and 5.4's `badgeState.ts` comment documents "for an active schedule the end is always later today". Whether 22:00→06:00 is user intent or bad data is a product decision the 5.3 spec did not capture. Fix requires redefining the evaluator's window semantics (wrap-around rule + the badge's end-instant math which assumes same-day end) — a frozen-contract renegotiation in both 5.3 and 5.4 territory, not a patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-5-schedule-enable-disable-and-removal-confirm-alert-not-password.md`
  summary: A staging action that clean-reverts its buffer to null while an Apply holding a non-null snapshot is still in flight lets the run's success handler commit the snapshot over the reverted buffer — the user's reverted (net-zero) intent is silently lost for that field.
  evidence: store.ts `apply()` captures per-field snapshots and its success handler clears/retains each buffer by reference identity (`s.stagedSchedules === schedulesSnapshot`). If `stageScheduleRemove`/`stageScheduleEnabledToggle`/`stageDomainRemove` clean-reverts to `null` mid-run (draft nets back to committed — e.g. delete a staged addition), `null !== snapshot`, so the run commits the snapshot's slice and the reverted state is discarded. Unreachable via the UI (row controls are `disabled={applyStatus === 'running'}`, pinned in Schedule.test.tsx) — direct-call/store-level only. The identical latent shape exists in `stageDomainRemove` (2-4) and `stageScheduleEnabledToggle` (5-1), so the fix is a family-level one (snapshot-aware guards in all staging actions or a queue-depth-aware applyStatus that also blocks staging), not a 5-5 patch. Surfaced by 5-5's review (VG-2/ECH-5).
